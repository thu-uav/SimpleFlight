# =============================================================================
# [修改记录 - 2026-06-15]
# 修改文件：train_datt0615.yaml（基于 train_datt.yaml）
# 修改内容：task 改为 TrackDATT0615（6D GT oracle：平动+转动扰动）
# =============================================================================
headless: true
use_local_usd: false

sim: ${task.sim}
env: ${task.env}

# model_dir: /root/SimpleFlight/scripts/outputs/track_datt/04-11_02-14/wandb/latest-run/files/checkpoint_1206124544.pt
model_dir: null
# model_dir: outputs/track_datt_6d/06-15_14-07/wandb/latest-run/files/checkpoint_final.pt
#/root/SimpleFlight/scripts/outputs/track_datt/04-25_17-01/wandb/latest-run/files/checkpoint_final.pt
#/root/SimpleFlight/scripts/outputs/track_datt/04-25_17-01/wandb/latest-run/files/checkpoint_209977344.pt
#/root/SimpleFlight/scripts/outputs/track_datt/04-25_17-01/wandb/latest-run/files/checkpoint_final.pt
#null
record_video: true
dist_horizontal_only: false  # [20260510] true=只保留XY扰动，false=全三轴扰动

total_frames: 1000_000_000
max_iters: -1
eval_interval: -1
save_interval: 100
seed: 0

viewer:
  resolution: [960, 720]
  eye: [0., 2.0, 2.5]
  lookat: [0., 0., 1.0]

wandb:
  group: ${oc.select:..task.name}
  run_name: track_datt_6d  # [0626] 6D扰动 + Jacobian Lipschitz 约束  之前：run_name: track_datt_6d
  entity: chen_zongy-tsinghua-university
  project: sim2real
  mode: online
  run_id:
  monitor_gym: True
  tags:

defaults:
  - task: TrackDATT0615   # [0615] 6D GT oracle 版本（平动+转动扰动）
  #  - task: TrackDATT0427  # 原3D版本
  - algo: mappo
  - _self_

algo:
  entropy_coef: 0.01
  clip_param: 0.1
