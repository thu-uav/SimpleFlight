# hydra.job.chdir: false

headless: true

sim: ${task.sim}
env: ${task.env}

# model_dir: /home/chenjy/OmniDrones/scripts/outputs/hover_targetrpy/08-13_20-00/wandb/run-20240813_200019-md9zkgyt/files/checkpoint_1494482944.pt
model_dir: null

total_frames: 3000_000_000 #1000_000_000
max_iters: -1
eval_interval: -1
save_interval: 100
seed: 0

viewer:
  resolution: [960, 720]
  # # front
  # eye: [0., -0.005, 6.]
  # lookat: [0., 0., 1.]
  eye: [0., 2.0, 2.5]
  lookat: [0., 0., 1.0]

wandb:
  group: ${oc.select:..task.name}
  run_name: Track_v_range1_0to1_5_smooth1_onlycritic_wodrag_z0
  # run_name: hover_wodrag_woall
  # run_name: debug
  job_type: train
  entity: chenjy
  project: simpleflight
  # mode: disabled
  mode: online # set to 'disabled' when debugging locally, online
  run_id: 
  monitor_gym: True
  tags: 

defaults:
  - task: Track
  # - task: Hover
  # - task: Rotate
  # - task: MultiGoto
  # - task: Goto_static
  # - task: Exchange
  - algo: mappo
  - _self_
