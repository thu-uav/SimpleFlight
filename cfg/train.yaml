# hydra.job.chdir: false

headless: true

sim: ${task.sim}
env: ${task.env}

# model_dir: /home/zanghongzhi/isaac_ws/OmniDrones/scripts/outputs/Track_slow_lr0/08-18_21-27/wandb/run-20240818_212757-b3ubojde/files/checkpoint_1494482944.pt
# model_dir: /home/jiayu/OmniDrones/scripts/outputs/Track_fast_wo_spinreward/10-17_13-44/wandb/run-20241017_134434-dzx2wnp2/files/checkpoint_1310982144.pt
model_dir: null

total_frames: 9000_000_000 #1000_000_000
max_iters: -1
eval_interval: -1
save_interval: 100
seed: 0

viewer:
  resolution: [960, 720]
  # # front
  # eye: [0., -0.005, 6.]
  # lookat: [0., 0., 1.]
  eye: [0., 2.0, 2.5]
  lookat: [0., 0., 1.0]

wandb:
  group: ${oc.select:..task.name}
  run_name: Track_slow_norm_smooth0_5
  # run_name: debug
  job_type: train
  entity: chenjy
  project: simpleflight
  # mode: disabled
  mode: online # set to 'disabled' when debugging locally, online
  run_id: 
  monitor_gym: True
  tags: 

defaults:
  - task: Track
  - algo: mappo
  - _self_
