# Smoothness-on ablation for TrackDATT0615.
# Inherits the original 6D translational+angular disturbance oracle config and
# only restores the SimpleFlight action-smoothness reward settings.
defaults:
  - TrackDATT0615
  - _self_

reward_action_smoothness_weight_init: 2.0
reward_action_smoothness_weight_lr: 0.0005
reward_smoothness_max: 2.0
